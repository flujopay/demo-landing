import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Habilita el modo estricto de React para detectar problemas en desarrollo
  reactStrictMode: true,
};

export default nextConfig;
